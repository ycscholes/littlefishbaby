import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

export default withRouter(({history}) => {
  console.log(history);

  return <div>fasdflasldkfjdsfasdfasdfasd</div>;
});
